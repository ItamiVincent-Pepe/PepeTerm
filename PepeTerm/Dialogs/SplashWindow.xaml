<Window x:Class="PepeTerm.Dialogs.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Width="450" Height="320"
        ShowInTaskbar="False"
        Topmost="True">

    <Border Background="#1E1E1E" CornerRadius="20" BorderBrush="#333333" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Лягушка -->
            <TextBlock Grid.Row="1"
                       Text="🐸"
                       FontSize="64"
                       HorizontalAlignment="Center"
                       Margin="0,20,0,0"/>

            <!-- Название -->
            <TextBlock Grid.Row="2"
                       Text="PepeTerm"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,15,0,0"
                       FontFamily="Segoe UI"/>

            <!-- Версия -->
            <TextBlock Grid.Row="3"
                       Text="v1.1 · The Developer's Powerhouse"
                       FontSize="13"
                       Foreground="#888888"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"
                       FontFamily="Segoe UI"/>

            <!-- Прогресс-бар -->
            <ProgressBar Grid.Row="4"
                         x:Name="LoadingBar"
                         Width="260"
                         Height="4"
                         IsIndeterminate="True"
                         Foreground="#0E639C"
                         Background="#333333"
                         BorderThickness="0"
                         Margin="0,25,0,30"/>
        </Grid>
    </Border>
</Window>